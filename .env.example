# GitHub Configuration
GITHUB_TOKEN=

# Telegram Configuration (optional)
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# Database Configuration
DB_CONNECTION_STRING=host=postgres user=postgres password=postgres dbname=issue_finder sslmode=disable port=5432

# General Settings
CHECK_INTERVAL=3600
MAX_ISSUES_PER_REPO=10
MAX_PROJECTS=50
LOG_DIR=logs
LOG_LEVEL=info
LOG_FORMAT=text

# Qualified Issue Settings
QUALIFIED_MIN_SCORE=0.6
QUALIFIED_TYPES=bug,feature,enhancement
QUALIFIED_EXCLUDE_LABELS=question,support,wontfix,duplicate,invalid
QUALIFIED_INCLUDE_LABELS=confirmed,triage/accepted,approved,help wanted
QUALIFIED_MIN_STARS=100
QUALIFIED_REQUIRE_APPROVAL=false

# Notification Settings
NOTIFY_LOCAL=true
NOTIFY_EMAIL=true
NOTIFY_EMAIL_MIN_SCORE=0.7
NOTIFY_MAX_PER_HOUR=5
NOTIFY_MAX_PER_DAY=20
NOTIFY_DIGEST_MODE=false
NOTIFY_DIGEST_INTERVAL=6h

# Anti-Spam
NEVER_NOTIFY_TWICE=true
CHECK_USER_COMMENTS=true
CHECK_USER_PRS=true

# Email Configuration (optional)
SMTP_HOST=
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
FROM_EMAIL=
TO_EMAIL=
EMAIL_MODE=instant
MAX_EMAILS_PER_HOUR=10
MAX_EMAILS_PER_DAY=50

# Anti-Spam Configuration
MAX_NOTIFICATIONS_PER_HOUR=10
DAILY_NOTIFICATION_LIMIT=30
MAX_NOTIFICATIONS_PER_PROJECT=2
NOTIFICATION_COOLDOWN_HOURS=24
DIGEST_MODE=false
DIGEST_TIME=09:00
CHECK_ISSUE_OPEN_BEFORE_NOTIFY=true
MAX_COMMENTS_PER_DAY=5
MAX_GITHUB_CALLS_PER_HOUR=4000

# Assignment Configuration
ASSIGNMENT_ENABLED=false
ASSIGNMENT_AUTO_MODE=false
ASSIGNMENT_MAX_DAILY=5
ASSIGNMENT_COOLDOWN_MINS=30
ASSIGNMENT_CHECK_ELIGIBILITY=true
ASSIGNMENT_AUTO_COMMENT=false

# Scoring Weights (optional - defaults shown)
SCORING_STAR_WEIGHT=0.08
SCORING_COMMENT_WEIGHT=0.15
SCORING_RECENCY_WEIGHT=0.15
SCORING_LABEL_WEIGHT=0.20
SCORING_DIFFICULTY_WEIGHT=0.12
SCORING_DESCRIPTION_WEIGHT=0.10
SCORING_ACTIVITY_WEIGHT=0.10
SCORING_MAINTAINER_WEIGHT=0.10
SCORING_CONTRIBUTOR_FRIENDLY_BONUS=0.15
SCORING_WEEKEND_BONUS=0.05
SCORING_MAX_SCORE=1.5

# Display Configuration
DISPLAY_MODE=partitioned
DISPLAY_MAX_GOOD_FIRST=15
DISPLAY_MAX_OTHER=10
DISPLAY_MAX_ASSIGNED=10
DISPLAY_SHOW_SCORE_BREAKDOWN=true

# Mode: good-first, actionable, go-upgrade, confirmed, both, or empty for continuous
MODE=
TARGET_REPO=

# MCP Server Configuration
MCP_SERVER_ENABLED=false
MCP_TRANSPORT=stdio
MCP_HTTP_PORT=8080
MCP_HTTP_HOST=localhost

# MCP Client Configuration
MCP_CLIENT_ENABLED=false

# MCP AI Enhancement
MCP_AI_ENHANCEMENT_ENABLED=false
MCP_ENHANCE_COMMENTS=true
MCP_SUGGEST_SOLUTIONS=true
MCP_ANALYZE_DIFFICULTY=true
MCP_AI_PROVIDER=claude

# API Keys for MCP AI Providers
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
